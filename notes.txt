Export
------
en pause, pas de soluce satisfaisante avec expo pur pour le moment :/
https://expo.canny.io/feature-requests/p/ability-to-save-files-on-internal-storage

pistes étudiées:
  expo MediaLibrary:
    fonctionne pour créer des fichiers/dossiers mais plutôt fait pour de la
    musique/images: créé les objects dans la galerie...
    - l'utilisateur ne peut pas choisir le dossier cible...
    - le bazar quand sauvegarde multiple: duplique les fichiers existants avec
    _n, aucun contrôle là-dessus
  expo Sharing:
    beautifully simple, one liner to export a file and prompt the user for an
    app able to handle it.
    Go! Coupled with .zip maker should be fine, now? :)
  raw react access to external storage? https://github.com/itinance/react-native-fs





WIP:

move loading full recipe to recipes screen when clicking on recipe row
rationale: better mini lag when clicking than when arriving on page

Flicker when arriving on a screen
---------------------------------
Hmm.
Even when waiting for all db results to arrive, setData functions &
sub-components are asynchronous. No obvious solution comes to mind. One big
state? -> Yeah, let's give it a try.

Seems to be working! :) :tada:
todo:
- finish migrating the rest of the screen to be like that (nb person + image)
- manage nbPerson from screen (fix edit & back not updating the value)
- refacto add/edit/.. screens
- move to TS?
- ...?
- profit


revisit ingredients: use a single field
  simpler algorithm:
    get first word
      if is number: ...
      else:
      // all ifs here can be beginning of word instead of first word
        if is xxx,xxx && x are digits, replace ',' with '.' & ...
        else if xxxAAAA: multiply xxx (for 8mg)
        else if xxx/xxx && x are digits: multiply numérateur & dénominateur (+simplify? :D)






way to browse the db to double check what is going on?
  ex: how can I save a 'bbb' quantity into a REAL field :mind-blown:


ingrédients
-----------
use case non géré: liste d'ingrédients / préparation pour 2 choses à faire pour
cette recette: ex gâteau + glaçage, ou plat + sauce. Hmm...
Right now kinda ok: lister ingreds à la suite, same pour la préparation


laisser un champ libre et multiplier/auto détecter les nombres/fractions...?
  -: possibilité de fail: e.g 1/ 2 -> 2/ 4
  +: plus de liberté

avoir une liste de quantité/unité de mesure/nom ?
  -: still possibilité de fail, bien que moindre (oeufs/... trucs insécables)
  -: structuré: e.g de la coriandre: on s'en fout de la quantité. Bruit visuel.
  +: structuré




gestion des erreurs: popup instead of console.log?




attach any kind of file?
e.g pdf,... + on click propose to open with phone eligible apps

https://material.io/design/color/the-color-system.html#color-theme-creation



# Photos Management
- stored on the file system

<recipe-uuid>
 |
 |-- 1.jpg // main
 |-- 2.jpg
 |-- ...

- can be exported from an existing photo or by taking one directly

# Durée
temps total = temps de préparation + temps de cuisson
